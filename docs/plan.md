# План работ по Tomato ViT (обновлено 11.12.2025 14:10)

## 1. Данные
- Проверить целостность `data/raw/plantvillage` и актуальность сплитов (`src/data/prepare_plantvillage.py`).
- Добавить быстрый sanity-check даталоадера (пары изображение/лейбл, размер, нормализация).
- При необходимости: сгенерировать ImageFolder-структуру через hardlink/copy для сторонних тулов.

## 2. Модель и обучение (ViT-tiny)
- Настроить конфиги Hydra под вит-tiny (трансформации, гиперпараметры, пути сплитов).
- Реализовать датасет/даталоадер на основе CSV (train/val).
- Запустить базовое обучение учителя (FP32/BF16 на XPU) с логами/чекпойнтами.
- Провести гиперпараметрический прогон (lr, warmup, weight decay, aug) при необходимости.

## 3. Оценка и репорты
- Посчитать метрики на val/test (accuracy, F1), confusion matrix.
- Сохранить метрики и графики (loss/accuracy) в `experiments/` с версией зависимостей.

## 4. Экспорт
- Экспортировать лучший чекпойнт в ONNX FP32 (динамическая/фиксированная форма 224x224).
- Проверить прогон ONNXRuntime CPU/XPU для валидации эквивалентности.

## 5. Квантизация INT8
- PTQ: собрать калибровочный сабсет, квантовать ONNX через ONNX Runtime или OpenVINO (под Arc).
- Оценить деградацию качества на test, зафиксировать метрики.
- (Отдельно) Подготовить TensorRT/ORT EP рецепт для NVIDIA, если потребуется перенос.

## 6. Деплой и интеграция
- Сформировать финальный артефакт для мобильного/десктоп (ONNX FP32 + INT8).
- Описать инференс-пайплайн (препроцесс, постпроцесс, соответствие id→класс).
- Проверить производительность на целевом железе (latency/throughput).

## 7. Поддержка и воспроизводимость
- Зафиксировать версии: Python, torch/torchvision, IPEX, timm, lightning, onnxruntime.
- Добавить инструкции по созданию окружения (CPU/GPU/XPU) и команду запуска обучения/экспорта.
- Вести дневник изменений и результаты экспериментов.
